{% set version = "2.2.6" %}

package:
  name: muparser
  version: {{ version }}

source:
  fn: v{{ version }}.tar.gz
  url: https://github.com/beltoforion/muparser/archive/v{{ version }}.tar.gz
  sha256: c124a00e73356204efa21bd2223db4b2a2b8a93cc25d16bae09e4fcaa82b620c
  patches:
    - no-console.patch

build:
  number: 1000

requirements:
  build:
    - cmake  # [win]
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}

test:
  commands:
    - test -f ${PREFIX}/include/muParser.h  # [unix]
    - if not exist %PREFIX%\\Library\\include\\muParser.h exit 1  # [win]
    - conda inspect linkages -p ${PREFIX} muparser  # [not win]
    - conda inspect objects -p ${PREFIX} muparser  # [osx]

about:
  home: http://muparser.beltoforion.de/
  license: MIT
  license_file: License.txt
  summary: A fast math parser library

extra:
  recipe-maintainers:
    - jschueller
